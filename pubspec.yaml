name: the_movie_db
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.3.8
  get_storage: ^2.0.3
  shimmer: ^2.0.0
  cached_network_image: ^3.1.0
  auto_size_text: ^3.0.0
  dio: ^4.0.0
  flutter_svg: ^0.22.0
  percent_indicator: ^3.0.1
  loading_gifs: ^0.3.0
  google_sign_in: ^5.1.0
  firebase_core: ^1.6.0
  firebase_analytics: ^8.3.3
  firebase_auth: ^3.1.3
  firebase_messaging: ^10.0.8
  fluttertoast: ^8.0.8
  carousel_slider: ^4.0.0
  sliver_fab: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  image_path: "assets/img/logo.jpeg"
  android: true
  ios: true

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/img/
